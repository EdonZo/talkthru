# Site images

- `og.png` — social preview, 1200x630.
- `ios-1-plus.jpg` … `ios-6-rec.png` — the six phone setup steps at full
  resolution, annotated by hand. **These are the originals — do not overwrite.**
- `web/` — half-dimension copies of the above, generated with
  `sips -Z <half-width>`. These are what the site and README actually reference
  (25 MB -> 808 KB). Regenerate them if an original changes.
